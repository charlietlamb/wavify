export default function ResourcesHeroContentHeading() {
  return (
    <div className="flex flex-col gap-2">
      <div className="relative">
        <span className="absolute -top-3 left-0 bg-gradient-to-r from-violet-500 to-violet-700 bg-clip-text text-3xl font-black uppercase italic text-transparent">
          Resources
        </span>
        <h1 className="relative z-10 bg-gradient-to-r from-zinc-100 to-zinc-200 bg-clip-text text-6xl font-black uppercase italic text-transparent">
          Resources
        </h1>
      </div>
    </div>
  )
}
